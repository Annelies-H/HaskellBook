--isPalindrome :: (Eq a) => [a] -> Bool
--isPalindrome x = x == reverse x

--myAbs :: Integer -> Integer
--myAbs x = if x > 0 
--            then x
--            else x * (-1)
            
--f :: (a,b) -> (c,d) -> ((b,d), (a,c))
--f x y = (,) t1 t2
--      where 
--        t1 = (,) (snd x) (fst y)
 --       t2 = (,) (fst x) (snd y)
        
--syntax ex1
--x = (+)
--g xs = x w 1
--     where w = length xs
     
-- syntax ex2
foo z = z


--syntax ex3
-- ??

--syntax ex4
--h a = fst a
